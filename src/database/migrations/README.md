# Migrations

SQL migration files live here. Naming convention: `NNNN_description.sql`
(e.g. `0001_create_categories.sql`). Applied in lexical order by the Phase 1
migration runner.

Phase 0 deliberately does not include a migration runner or schema.
